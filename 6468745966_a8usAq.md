AI算力需求持续攀升，消息称AWS要求工程师清理闲置EC2实例--2026年08月08日00时13分19秒

<article class="news-item">
<h1>AI算力需求持续攀升，消息称AWS要求工程师清理闲置EC2实例</h1>
<p class="news-meta">分类：RSS · 来源：IT之家</p>
<div class="news-body">AI算力需求持续攀升，消息称AWS要求工程师清理闲置EC2实例
<p>据IT之家8月7日消息，《The Information》报道称，亚马逊云科技（AWS）近期要求工程师关闭不再使用的云计算实例，重点涉及闲置的EC2实例。随着人工智能应用持续消耗计算资源，云服务商开始通过提高资源利用率，缓解算力和基础设施承载压力。</p>
<h2>闲置实例成为资源优化重点</h2>
<p>EC2是AWS提供的弹性云服务器服务，用户可以根据业务需要创建不同配置的虚拟机，并按需使用计算、存储和网络资源。对于开发测试、临时任务以及已经结束的项目而言，相关实例如果没有及时释放，仍可能占用云平台的计算容量。</p>
<p>报道称，AWS此次提出的要求主要针对这类不再使用的EC2实例。其核心并不是简单扩大服务器规模，而是先识别长期处于低负载状态的资源，并推动相关实例停止运行或关闭，以减少计算能力被闲置的情况。</p>
<p>这一做法与当前云计算市场面临的资源压力有关。人工智能模型的训练和推理需要大量计算资源，其中搭载GPU的云实例尤其重要。随着大语言模型及相关应用快速发展，云服务商需要在持续扩充基础设施的同时，尽可能提高现有资源的使用效率。</p>
<h2>约六成EC2实例平均CPU利用率偏低</h2>
<p>报道援引的数据称，在一个为期30天的测量周期内，AWS约65%的EC2实例平均CPU利用率低于20%。这意味着，大量实例虽然处于运行状态，但实际承担的计算任务相对有限。</p>
<p>低CPU利用率并不必然说明实例完全没有价值。部分业务可能需要为突发流量预留容量，某些服务也可能主要依赖内存、磁盘或网络，而非持续占用CPU。不过，当实例长期处于低负载状态，且业务已经结束或迁移，继续运行就可能造成资源浪费，也会增加用户的使用成本。</p>
<p>因此，AWS正在升级计算优化器（Compute Optimizer）。该工具能够自动分析服务器在14天内的CPU使用率和IO数据，并对部分低利用率虚拟机进行标记。报道提到，峰值CPU利用率低于15%、且网络流量非常低的虚拟机，将成为重点识别对象。</p>
<h3>自动分析并不等于直接关停</h3>
<p>从功能描述看，计算优化器首先承担的是监测、分析和提示作用。它根据一段时间内的运行数据，帮助识别可能存在闲置或配置过高的实例。对于企业用户而言，是否停止实例、调整规格或保留资源，仍需要结合业务连续性、突发流量和数据安全等因素判断。</p>
<p>这也说明，云资源管理正在从单纯的容量扩张，转向更精细的使用分析。通过对CPU、IO和网络流量进行综合评估，平台可以为资源回收和规格调整提供依据，避免仅凭单一指标判断实例是否应该关闭。</p>
<h2>AWS持续增加电力容量，AI带来更高基础设施压力</h2>
<p>在清理闲置实例之外，AWS也在持续建设新的基础设施。报道称，AWS过去一年新增了3.8吉瓦的电力容量，规模相当于为280万户家庭提供电力。</p>
<p>电力容量的增加反映出云计算数据中心对能源和基础设施的需求正在扩大。服务器、网络设备以及配套制冷系统都需要稳定的电力供应，而AI计算集群往往包含大量高性能GPU，其功耗和部署密度也使数据中心面临更高要求。</p>
<p>因此，云服务商需要同时处理两方面问题：一方面建设更多数据中心、采购更多服务器和GPU，以满足人工智能训练与推理需求；另一方面则要减少已经分配但没有得到充分使用的资源，从现有基础设施中释放更多可用容量。</p>
<h2>云计算竞争焦点延伸至资源效率</h2>
<p>过去，云平台竞争更多体现在计算实例种类、价格、服务覆盖范围和可靠性等方面。随着AI需求迅速增长，GPU供应、电力条件、数据中心建设进度以及资源调度能力，也成为影响云服务商承载能力的重要因素。</p>
<p>对AWS而言，要求工程师关闭闲置实例，体现出其在资源紧张环境下的管理思路：在扩容之外，先处理长期未被有效利用的计算资源。对于企业客户来说，这也提醒用户定期检查云环境中的实例状态，区分生产服务、备用容量、开发测试资源和已经结束的任务。</p>
<ul>
<li>及时释放已经完成任务的EC2实例，避免其持续占用计算资源；</li>
<li>根据CPU、IO和网络流量等指标，检查实例是否存在长期低负载情况；</li>
<li>对确实需要保留的备用资源，结合业务峰值和服务连续性要求进行评估；</li>
<li>在调整或关闭实例前，确认数据、依赖服务和恢复方案不会受到影响。</li>
</ul>
<p>目前，报道并未显示AWS会对所有低利用率实例进行统一关停。更明确的变化是，平台正在强化对闲置资源的识别，并要求相关工程师主动减少不必要的资源占用。随着AI继续推动算力需求增长，如何在扩充容量与提升利用率之间取得平衡，可能成为云计算服务商需要长期面对的问题。</p></div>
</article>